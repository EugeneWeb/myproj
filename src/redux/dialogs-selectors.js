export const getDialogs = (state) => {
    return state.dialogsPage.dialogs
}

export const getMessages = (state) => {
    return state.dialogsPage.messages
}

export const getNewMessageBody = (state) => {
    return state.dialogsPage.newMessageBody
}